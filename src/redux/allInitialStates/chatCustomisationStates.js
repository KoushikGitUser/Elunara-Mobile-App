export const chatCustomisationStates = {
  // LLM Preference
  selectedLLM: {
    id: null, // null for "Auto"
    name: "Auto",
  },

  // Response Style
  selectedResponseStyle: {
    id: null, // null for "Auto"
    name: "Auto",
  },

  // Response Language
  selectedLanguage: {
    id: null, // null for default (English)
    name: "English",
  },

  // Citation Format
  selectedCitationFormat: {
    id: null, // null for default (Harvard)
    name: "APA",
  },
};
